# Override the stock RV1106 family boot flow so Luckfox boards use the
# Armbian bootscript path, which already has working overlay semantics.
SRC_EXTLINUX="no"
BOOTSCRIPT="boot-rv1106.cmd:boot.cmd"
FORCE_BOOTSCRIPT_UPDATE="yes"
BOOT_OVERLAY_MODE="armbianEnv"
